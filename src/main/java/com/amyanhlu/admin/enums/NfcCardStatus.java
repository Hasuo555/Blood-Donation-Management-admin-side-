package com.amyanhlu.admin.enums;

/**
 * Matches {@code nfc_cards.status} enum:
 * ACTIVE, EXPIRED, LOST, BLOCKED, REPLACED.
 */
public enum NfcCardStatus {
    ACTIVE,
    EXPIRED,
    LOST,
    BLOCKED,
    REPLACED
}
